require 'spec_helper'

describe IcelabCrm do
  it 'has a version number' do
    expect(IcelabCrm::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
